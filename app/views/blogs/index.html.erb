<p style="color: green"><%= notice %></p>

<h1>Blogs</h1>
<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Body</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% @blogs.each do |blog| %>
      <tr>
        <td><%= blog.title %></td>
        <%
=begin%>
 <td><%= blog.body %></td> 
<%
=end%>
        <td><%= link_to 'Show', blog %></td>
        <td><%= link_to 'Edit', edit_blog_path(blog)%></td>
        <td><%= link_to 'Delete', blog, method: :delete %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<%
=begin%>
 
<div id="blogs">
  <% @blogs.each do |blog| %>
    <%= render blog %>
    <p>
      <%= link_to "Show this blog", blog %>
    </p>
  <% end %>
</div> 
<%
=end%>
<%= link_to "New blog", new_blog_path %>
